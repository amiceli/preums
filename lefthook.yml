commit-msg:
  commands:
    lint:
      run: npx --no -- commitlint --edit $1

pre-commit:
  commands:
    biome:
      glob: "*.{js,ts,cjs,mjs,d.cts,d.mts,jsx,tsx,json,jsonc}"
      run: npx biome check --no-errors-on-unmatched --files-ignore-unknown=true {staged_files}
    pint:
      glob: "*.{php}"
      run: ./vendor/bin/pint --bail {staged_files}
    gitleaks:
      run: gitleaks git .
